services:
  reacher:
    image: reacherhq/backend:latest
    container_name: reacher-email-verifier
    ports:
      - "8080:8080"
    environment:
      - RUST_LOG=info
      - RCH__FROM_EMAIL=test@example.org
    restart: unless-stopped